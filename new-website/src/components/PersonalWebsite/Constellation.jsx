import React from 'react'

export default function Constellation({ isDark }) {
  // same implementation as before, returning stars & lines
  return <div className="absolute inset-0 pointer-events-none">Constellation placeholder</div>
}